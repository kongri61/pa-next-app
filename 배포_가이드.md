# 배포 가이드 (사용자용)

## 🚀 빠른 배포 방법

### 방법 1: 배치 파일 사용 (가장 간단)

1. **파일 탐색기**에서 프로젝트 폴더 열기
2. **`deploy-all.bat`** 파일을 **더블클릭**하여 실행

   또는

3. **PowerShell** 또는 **명령 프롬프트**에서:
   ```cmd
   deploy-all.bat
   ```

이 파일이 자동으로 다음을 수행합니다:
- ✅ 프로젝트 빌드
- ✅ Git 커밋
- ✅ GitHub 푸시
- ✅ Vercel 배포

---

### 방법 2: 단계별 수동 배포

#### 1단계: 프로젝트 빌드
```cmd
npm run build
```

#### 2단계: Git에 커밋 및 푸시
```cmd
git add .
git commit -m "사진 페이지 넘김 표시 위치 변경"
git push origin main
```

#### 3단계: Vercel 배포
```cmd
npx vercel --prod --yes
```

---

### 방법 3: 메뉴 방식 (run.bat 사용)

1. **`run.bat`** 파일을 더블클릭
2. 메뉴에서 **"1. 자동 배포"** 선택

---

## 📋 배포 전 확인사항

### Git 저장소 설정 확인
```cmd
git remote -v
```
원격 저장소가 설정되어 있지 않다면:
```cmd
npm run setup:git
```
또는 직접 설정:
```cmd
git remote add origin YOUR_REPO_URL
```

### Vercel 로그인 확인
```cmd
npx vercel whoami
```
로그인되어 있지 않다면:
```cmd
npx vercel login
```

---

## ⚠️ 문제 해결

### PowerShell 한글 경로 오류
배치 파일을 직접 더블클릭하거나, 명령 프롬프트(cmd)를 사용하세요.

### Git 저장소를 찾을 수 없음
```cmd
git init
npm run setup:git
```

### 빌드 실패
```cmd
npm install
npm run build
```

### 배포 실패
- Vercel CLI가 설치되어 있는지 확인: `npx vercel --version`
- Vercel에 로그인되어 있는지 확인: `npx vercel whoami`

---

## 🎯 추천 방법

**가장 간단한 방법:**
1. 파일 탐색기에서 프로젝트 폴더 열기
2. `deploy-all.bat` 파일 더블클릭
3. 완료!

---

## 📝 배포 후 확인

배포가 완료되면 Vercel에서 제공하는 URL로 접속하여 변경사항을 확인하세요.

일반적인 배포 URL:
- `https://pa-realestate-b2chnfoip-paproperty.vercel.app`

