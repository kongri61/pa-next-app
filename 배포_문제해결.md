# Git Push 오류 해결 방법

## 현재 상황
원격 저장소에 로컬에 없는 변경사항이 있어서 push가 거부되었습니다.

## 해결 방법

### 방법 1: 원격 변경사항 가져오기 후 푸시 (권장)

```cmd
git pull origin main
git push origin main
```

또는 rebase 사용 (더 깔끔한 히스토리):

```cmd
git pull --rebase origin main
git push origin main
```

### 방법 2: 강제 푸시 (주의: 원격 변경사항이 덮어써집니다)

⚠️ **주의**: 이 방법은 원격 저장소의 변경사항을 덮어씁니다. 
다른 사람과 협업 중이라면 사용하지 마세요!

```cmd
git push origin main --force
```

### 방법 3: 원격 변경사항 확인 후 병합

```cmd
git fetch origin
git log HEAD..origin/main
git pull origin main
git push origin main
```

## 추천 순서

1. **원격 변경사항 확인**:
   ```cmd
   git fetch origin
   git log HEAD..origin/main
   ```

2. **변경사항 가져오기**:
   ```cmd
   git pull --rebase origin main
   ```

3. **충돌이 있다면 해결 후**:
   ```cmd
   git add .
   git rebase --continue
   ```

4. **푸시**:
   ```cmd
   git push origin main
   ```

5. **Vercel 배포**:
   ```cmd
   npx vercel --prod --yes
   ```


